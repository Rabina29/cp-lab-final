#include <bits/stdc++.h>
using namespace std;

int binToDec(string s) {
    int val = 0;
    for (char c : s) {
        val = val * 2 + (c - '0');
    }
    return val;
}

int main() {
    int T;
    cin >> T;

    for (int tc = 1; tc <= T; tc++) {

        string decIp;
        cin >> decIp;

        stringstream ss(decIp);
        string part;
        vector<int> decParts;
        while (getline(ss, part, '.')) {
            decParts.push_back(stoi(part));
        }

        string b1, b2, b3, b4;
        cin >> b1 >> b2 >> b3 >> b4;

        vector<int> binParts = {
            binToDec(b1),
            binToDec(b2),
            binToDec(b3),
            binToDec(b4)
        };

        bool same = true;
        for (int i = 0; i < 4; i++) {
            if (decParts[i] != binParts[i]) {
                same = false;
                break;
            }
        }

        cout << "Case " << tc << ": " << (same ? "Yes" : "No") << "\n";
    }

    return 0;
}
